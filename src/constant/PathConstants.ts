class PathConstants {
	ROOT = 'localhost:3000'
	LOGIN = `/`
	MENU = `/menu`
	LEARN = `/learn`
	LESSON = `/lesson`
	QUIZ = `/quiz`
	ACHIEVEMENTS = `/achievements`
}

export default new PathConstants()
